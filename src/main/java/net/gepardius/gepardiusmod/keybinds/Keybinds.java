package net.gepardius.gepardiusmod.keybinds;

import com.mojang.blaze3d.platform.InputConstants;
import net.gepardius.gepardiusmod.GepardiusMod;
import net.minecraft.client.KeyMapping;
import net.minecraftforge.client.ClientRegistry;

import javax.swing.text.JTextComponent;

/*public class Keybinds {

    private Keybinds(){}

    public static KeyMapping exampleKeyMapping;

    public static void init(){
        exampleKeyMapping = registerKey("eyample_key",KeyMapping.CATEGORY_GAMEPLAY, InputConstants.KEY_R);
    }

    private static KeyMapping registerKey(String name, String category, int keycode){
        final var key = new KeyMapping("key" + GepardiusMod.MOD_ID + "." + name, keycode, category);
        ClientRegistry.registerKeyBinding(key);
        return  key;
    }
}*/
